import numpy as np

class Config(object):
    # ---------------------------------------------------------
    wandb = True
    # ---------------------------------------------------------
    train_gpu = [0, ]
    # ---------------------------------------------------------
    num_classes = 4
    # ---------------------------------------------------------
    backbone = "ConvNeXtTiny"
    # ---------------------------------------------------------
    model_path = 'pretrained/convnext_tiny.h5'
    # ---------------------------------------------------------
    input_shape = [256, 256]
    # ---------------------------------------------------------
    Init_Epoch = 0
    Freeze_Epoch = 50
    Freeze_batch_size = 4 # 4
    # ---------------------------------------------------------
    UnFreeze_Epoch = 150
    Unfreeze_batch_size = 2 #2
    # ---------------------------------------------------------
    Freeze_Train = True
    # ---------------------------------------------------------
    Init_lr = 1e-4
    Min_lr = Init_lr * 0.01
    # ---------------------------------------------------------
    optimizer_type = "adam"
    momentum = 0.9
    weight_decay = 0
    # ---------------------------------------------------------
    lr_decay_type = 'cos'
    # ---------------------------------------------------------
    save_period = 5
    # ---------------------------------------------------------
    save_dir = 'logs'
    # ---------------------------------------------------------
    eval_flag = True
    eval_period = 5
    # ---------------------------------------------------------
    VOCdevkit_path = 'VOCdevkit'
    # ---------------------------------------------------------
    unlabeled_id_path = 'splits/pascal/unlabeled.txt'
    # ---------------------------------------------------------
    dice_loss = True
    # ---------------------------------------------------------
    focal_loss = False
    # ---------------------------------------------------------
    cls_weights = np.ones([num_classes], np.float32)
    # ---------------------------------------------------------
    num_workers = 1
    # ---------------------------------------------------------


config = Config()